services:
  - type: web
    name: memora
    runtime: python
    buildCommand: pip install -r requirements.txt
    startCommand: python memora_cloud.py
    envVars:
      - key: PORT
        value: 10000
    disk:
      name: memora-data
      mountPath: /data
      sizeGB: 1
